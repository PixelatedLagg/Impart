namespace Impart
{
    /// <summary>All types of changes in the Animation.</summary>
    public enum ChangeType
    {
        BackgroundColor,
        ForegroundColor,
        Width,
        Height,
        Position,
        Size
    }
}