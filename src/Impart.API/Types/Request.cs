namespace Impart.Api
{
    /// <summary>All of the request types.</summary>
    public enum RequestType
    {
        Get,
        Push,
        Put,
        Delete,
        Head,
        Connect,
        Options,
        Trace,
        Patch
    }
}