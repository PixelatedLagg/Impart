namespace Impart.Scripting
{
    internal static class Initialize
    {
        internal static void Start()
        {
            //initialize everything needed to execute code in browser
        }
    }
}